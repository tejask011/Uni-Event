name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  # ── Update Dependencies via PR ────────────────────────────────────
  update-dependencies:
    name: Update Dependencies
    runs-on: ubuntu-latest
    if: github.event_name == 'push'
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901ebb512078596e6fc4b050511d3e5ab4

      - name: Set up Node.js 20
        uses: actions/setup-node@60edb3dd545a775cdb01d3efedbac2d7e5d13b8e
        with:
          node-version: 20

      - name: Update app dependencies
        working-directory: ./app
        run: npm ci

      - name: Update cloud-functions dependencies
        working-directory: ./cloud-functions
        run: npm ci

      - name: Create Pull Request for dependency updates
        uses: peter-evans/create-pull-request@70a41aba183996c44cedce9e0aded0624ffb39f7
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: "chore: update package-lock.json [skip ci]"
          branch: chore/update-dependencies
          title: "chore: update package-lock.json files"
          body: |
            Automated dependency updates for both app and cloud-functions.
            This PR was created automatically by the CI workflow.
          delete-branch: true
          labels: dependencies, automated

  # ── App: Lint + Test ─────────────────────────────────────────────
  lint-and-test:
    name: Lint & Test (app)
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    defaults:
      run:
        working-directory: ./app
    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901ebb512078596e6fc4b050511d3e5ab4

      - name: Set up Node.js 20
        uses: actions/setup-node@60edb3dd545a775cdb01d3efedbac2d7e5d13b8e
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: app/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Auto-format with Prettier
        run: npx prettier --write .

      - name: Create PR for formatted files
        if: github.event_name == 'push'
        uses: peter-evans/create-pull-request@70a41aba183996c44cedce9e0aded0624ffb39f7
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: "style: auto-format with prettier"
          branch: auto-format/prettier
          title: "style: auto-format with prettier"
          body: |
            Automated Prettier formatting applied.
            This PR was created automatically by the CI workflow.
          delete-branch: true
          labels: formatting, automated

      - name: Run ESLint
        run: npm run lint

      - name: Run Jest tests
        run: npm test -- --ci --passWithNoTests
